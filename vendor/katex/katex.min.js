(function(){
  if (window.katex) return;
  window.katex = {
    render: function(input, element){
      if (!element) return;
      element.textContent = input;
      element.setAttribute('data-katex-fallback', 'true');
    }
  };
})();
